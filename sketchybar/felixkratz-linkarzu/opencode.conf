#!/usr/bin/env bash

# primary: pin the popup to the macOS primary display
# active: use SketchyBar's native popup on the cursor-active display
# all: show a popup on every display
# A display name or UUID targets that specific display.
OPENCODE_POPUP_TARGET="${OPENCODE_POPUP_TARGET:-primary}"
